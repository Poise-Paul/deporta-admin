import { TripDetailView } from "@/components/trips/TripDetailsPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <TripDetailView />;
};

export default page;
